{% extends "_layouts/base.jinja2" %}

{% block title %}{{ page.title }}{% endblock %}
{% block extrahead -%}
  {% if page.description -%}
  <meta name="description" content="{{ page.description }}" />
  {%- endif %}
  <link rel="canonical" href="{{ canonical_url(site, page) }}" />
{%- endblock %}

{% block body -%}
  <article class="prose">
    {{ page.content|safe|indent(4) }}
  </article>
{%- endblock %}
